[build]
builder      = "nixpacks"
buildCommand = "pnpm install && pnpm --filter api build"
watchPatterns = ["packages/api/**", "packages/shared/**", "pnpm-lock.yaml"]

[deploy]
startCommand  = "node packages/api/dist/index.js"
healthcheckPath    = "/api/health"
healthcheckTimeout = 30
restartPolicyType  = "on_failure"
